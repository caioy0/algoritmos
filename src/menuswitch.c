#include <stdio.h>

int main(){
    int opcao;
    switch(opcao) 
    {
        case 1:
            // código a ser executado se a opcao for igual a 1
            break;
        case 2:
            // código a ser executado se a opcao for igual a 2
            break;
        // outros casos...
        default:
            // código a ser executado se nenhum dos valores anteriores for igual à opcao
            break;
    }
    return 0;
}